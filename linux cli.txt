~ : home directory
ls : list , -l to see owners/permissions,dates and more (-1h to make file sizes readable to mb,kb etc)
pwd: print working directory tell me where i am
-1
-al : hidden files
cd: open ex cd documents -> then use cd again to go back
whoami
uname -a: system info
uname: os only
df -h: disk spaces
cat: get more info on a specific file folder
ssh
history: see what the usser has done
su: switch ussseddr login to another user use -l to be more specific
gobuster : tool pen testers to scan and find hidden web pages
   -> dir : directory specifies it
   -> --url : set the target site url
   -> -w : Specifies the wordlist Gobuster will use to guess directory and file names

         example: gobuster dir --url http://www.onlineshop.thm/ -w /usr/share/wordlists/dirbuster/directory-list.txt
hydra : tool pen testers use to try multiple password ( a .txt with password is needed in order to do this)
 -> -l admin : Attempts to log in using the username admin
 -> -P passlist.txt :  Specifies the password list to try (ofc isnt always passlist.txt)
 -> www.onlineshop.thm : Sets the target website
 -> http-post-form : Indicates that this is an HTTP POST request form
 -> "/login:username=^USER^&password=^PASS^:F=incorrect" : Specifies how the login request is sent and how Hydra determines whether a login attempt has failed
 -> -V : Enables verbose output, which displays each username and password attempted

man : manual, if i dont know a certain commandi can do ->  man the command i dont know
echo: print text that ive writen
 -> &
 -> &&
 -> >
 -> >>
find -name ___ : i can also search for all the txt files or any file type for ex find -name *.txt  , i must use *
wc : cout the number of entries in a .log
grep: find specific things in the hundreds of logs easily
     ex: grep "1.111.11.1" access.log
     -> -R: recursive,search all files and subfolders inside a directory 
       ex: grep -R "THM" access.log /home/tryhackme/


touch:	touch	Create file
mkdir:	make directory	Create a folder
cp:	copy	Copy a file or folder
mv:	move	Move a file or folder can use to rename also
rm:	remove	Remove a file or folder -R to remove a directory
file:	file	Determine the type of a file

nano: create file( text editor)
wget: download file from web
scp: transfer and copy files between computers with ssh
  -> let's copy an example file from our machine to a remote machine
  ex: ssh important.txt ubuntu@192.168.1.30 /home/ubuntu/transferred.txt
  -> now let's reverse this and layout the syntax for using scp to copy a file from a remote computer that we're not logged into
  ex:ssh ubuntu@192.168.1.30:/home/ubuntu/documents.txt notes.txt
python3 -m  http.server
ps: see how many proccesses are running like task manager on windows
 -> ps aux to see processes fom other users,system etc exactly like task manager
top: top 10 processes running or your system in realtime
kill: terminate a proccess such as end task in task manager by providing the id of it (ex: kill 500)
 --Below are some of the signals that we can send to a process when it is killed:
   SIGTERM - Kill the process, but allow it to do some cleanup tasks beforehand
   SIGKILL - Kill the process - doesn't do any cleanup after the fact
   SIGSTOP - Stop/suspend a process
systemctl: allows us to interact with the systemd process
 --format: systemctl [option] [service]
   -> to tell apache to start up, we'll use 
       -systemctl start apache2 (this starts apache2 now, enable starts it when we boot up our pc)
   ->Same with if we wanted to stop apache, we'd just replace the [option] with stop (instead of start like we provided)
        We can do five options with systemctl:
Start
Stop
Enable
Disable
Status



fg:ommand is being used to bring the background process back into use on the terminal, where the output of the script is now returned to us.
crontab: crontab -e   -e to open an up an editor to edit it  
  -> automate tasks with it choose specifc dates hours etc 
     -https://tryhackme.com/room/linuxfundamentalspart3
     -https://crontab-generator.org/

apt:add,remove,update software ex: apt update
  -> add-apt-repository : and to remove this file : add-apt-repository --remove
  -> apt remove [software-name-here] : once removed
